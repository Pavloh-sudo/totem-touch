import 'package:flutter/material.dart';

import 'app_colors.dart';

abstract final class AreaColors {
  static const robotics = AppColors.techCyan;
  static const cutting = Color(0xFFD99324);
  static const manufacturing = Color(0xFF5865D8);
  static const machinery = Color(0xFF527B92);
  static const software = Color(0xFF7657C7);
  static const careers = AppColors.successGreen;

  static const double maximumCardCoverage = 0.15;
  static const double glowOpacity = 0.06;
}
